import { SingleAlphaNumFormControl } from './single-alpha-num-form-control';

describe('SingleAlphaNumFormControl', () => {
  it('should create an instance', () => {
    expect(new SingleAlphaNumFormControl()).toBeTruthy();
  });
});
